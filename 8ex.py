formatter = "{} {} {} {}"

print(formatter.format("N","A","I","M"))
print(formatter.format(1,2,3,5))

print(formatter.format(
    'Rack in money',
    'Getting them chains',
    'do one or another',
    'its all the same'
))